/* This file contains references to third party (vendor) libraries.
This is used by Webpack in the production build only. This avoids users to
download a big JS file every time a line of code changes as the third library
code tends to change less often than the application code. */

/* eslint-disable no-unused-vars */
import fetch from 'whatwg-fetch';
